# ─────────────────────────────────────────────────────────────
# ares-api · Docker Compose (stub)
# ─────────────────────────────────────────────────────────────
# Full stack: ares-api + PostgreSQL (+ optional Redis).
# This is a commented outline — the team should uncomment and
# adjust once the Dockerfile is finalized.
#
# version: "3.9"
#
# services:
#   # ── ares-api ────────────────────────────────────────────
#   ares-api:
#     build: .
#     ports:
#       - "8000:8000"
#     env_file:
#       - .env
#     depends_on:
#       postgres:
#         condition: service_healthy
#     restart: unless-stopped
#
#   # ── PostgreSQL ──────────────────────────────────────────
#   postgres:
#     image: postgres:16
#     ports:
#       - "5432:5432"
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: promptstrike
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U postgres"]
#       interval: 5s
#       timeout: 5s
#       retries: 5
#     restart: unless-stopped
#
#   # ── Redis (optional — uncomment when needed) ───────────
#   # redis:
#   #   image: redis:7-alpine
#   #   ports:
#   #     - "6379:6379"
#   #   restart: unless-stopped
#
# volumes:
#   postgres_data:
